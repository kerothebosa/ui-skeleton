import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"architecture.md","filePath":"architecture.md"}');
const _sfc_main = { name: "architecture.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h1><h2 id="module-map" tabindex="-1">Module Map <a class="header-anchor" href="#module-map" aria-label="Permalink to &quot;Module Map&quot;">​</a></h2><ul><li><code>src/core/</code><ul><li><code>enhancer.ts</code>: lifecycle control, request tracking, timeout/timing logic, event emission.</li></ul></li><li><code>src/network/</code><ul><li><code>global-patch-registry.ts</code>: shared global monkey-patch ownership for <code>fetch</code> and <code>XMLHttpRequest</code>.</li><li><code>fetch-interceptor.ts</code>, <code>xhr-interceptor.ts</code>: subscribe/unsubscribe wrappers around registry.</li></ul></li><li><code>src/dom/</code><ul><li><code>skeleton-manager.ts</code>: skeleton node show/hide/cleanup behavior with adaptive placeholder generation.</li><li><code>style-registry.ts</code>: runtime CSS variable/keyframe generation for visual presets.</li></ul></li><li><code>src/styles/</code><ul><li><code>skeleton.css</code>: published default overlay styles consumed by <code>@skeleton-ui/net/styles.css</code>.</li></ul></li><li><code>src/orchestrator/</code><ul><li><code>event-bus.ts</code>: typed internal event fan-out.</li></ul></li><li><code>src/types/</code><ul><li><code>public.ts</code>: public API surface and event payload contracts.</li></ul></li></ul><h2 id="request-lifecycle-flow" tabindex="-1">Request Lifecycle Flow <a class="header-anchor" href="#request-lifecycle-flow" aria-label="Permalink to &quot;Request Lifecycle Flow&quot;">​</a></h2><ol><li>Interceptor installs and subscribes to global patch registry.</li><li>Registry emits normalized network events (<code>request:start</code>, <code>request:end</code>, <code>error</code>) to active subscribers.</li><li><code>SkeletonEnhancer</code> receives these events and applies: <ul><li>request filtering (<code>shouldHandleRequest</code>)</li><li>show delay (<code>showDelayMs</code>)</li><li>timeout synthesis (<code>requestTimeoutMs</code>)</li><li>minimum visible duration (<code>minVisibleMs</code>)</li></ul></li><li><code>SkeletonEnhancer</code> emits typed library events through <code>EventBus</code>.</li><li><code>SkeletonManager</code> mutates DOM state and overlay node.</li><li>Style registry resolves animation/theme tokens and injects instance-scoped CSS.</li></ol><p>Adaptive rendering safety:</p><ul><li>adaptive candidate scanning tolerates empty/invalid selector lists</li><li>if adaptive placeholder generation fails, manager falls back to overlay mode instead of dropping the skeleton node</li><li>this keeps <code>skeleton:show</code> and visible DOM state consistent under bad config input</li></ul><h2 id="styling-contract" tabindex="-1">Styling Contract <a class="header-anchor" href="#styling-contract" aria-label="Permalink to &quot;Styling Contract&quot;">​</a></h2><ul><li>Runtime only injects DOM nodes and class names.</li><li>Visual appearance uses both shipped stylesheet (<code>@skeleton-ui/net/styles.css</code>) and runtime-generated CSS.</li><li>Consumers can override by: <ul><li>setting <code>overlayClassName</code>, and/or</li><li>setting <code>skeletonVisuals</code> (mode, animation, theme, adaptive scanning), and/or</li><li>overriding CSS in their own stylesheet.</li></ul></li></ul><h2 id="multi-instance-safety" tabindex="-1">Multi-Instance Safety <a class="header-anchor" href="#multi-instance-safety" aria-label="Permalink to &quot;Multi-Instance Safety&quot;">​</a></h2><ul><li>Global patching is reference-counted.</li><li>The first active subscriber installs patches.</li><li>Additional enhancer instances reuse the same patches.</li><li>Patches are restored only when the last subscriber unsubscribes.</li><li>This prevents patch clobbering between multiple enhancer instances.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("architecture.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const architecture = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  architecture as default
};
